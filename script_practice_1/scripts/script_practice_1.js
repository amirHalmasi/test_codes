// even number from 0 to ? (any number you want)
const numberTo = 1000;
for (let index = 0; index <= numberTo; index++) {
  if (index % 2 == 0) {
    console.log(`${index} is an even number`);
  }
}
